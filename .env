# Server configuration
SERVER_ADDRESS=:8080

# Database configuration (for SQLite)
DB_PATH=./data/dnd_characters.db

# Secret key for securing the application (e.g., JWT, sessions)
SECRET_KEY=your-secret-key

# Example: If you want to configure a different database for production
# Uncomment the below lines and configure accordingly
# DATABASE_URL=postgres://username:password@localhost/dbname?sslmode=disable
